{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "rm -rf node_modules .next ~/.npm ~/.pnpm-store && npm install -g npm@10.8.2 && npm ci --no-audit --no-fund --cache /tmp/npm-cache"
}